diff --git a/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/CustomToolbar.kt b/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/CustomToolbar.kt
--- a/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/CustomToolbar.kt
+++ b/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/CustomToolbar.kt
@@ -168,10 +168,19 @@
         // We want to handle display cutout always, no matter the HeaderConfig prop values.
         // If there are no cutout displays, we want to apply the additional padding to
         // respect the status bar.
+        // Patched: fall back to the system status_bar_height resource when WindowInsets
+        // reports 0 (common on Android emulators without a display cutout).
+        var topInset = max(cutoutInsets.top, if (shouldApplyTopInset) systemBarInsets.top else 0)
+        if (topInset == 0 && shouldApplyTopInset) {
+            val resId = resources.getIdentifier("status_bar_height", "dimen", "android")
+            if (resId > 0) {
+                topInset = resources.getDimensionPixelSize(resId)
+            }
+        }
         val verticalInsets =
             InsetsCompat.of(
                 0,
-                max(cutoutInsets.top, if (shouldApplyTopInset) systemBarInsets.top else 0),
+                topInset,
                 0,
                 max(cutoutInsets.bottom, 0),
             )
diff --git a/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/bottomsheet/SheetDelegate.kt b/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/bottomsheet/SheetDelegate.kt
--- a/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/bottomsheet/SheetDelegate.kt
+++ b/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/bottomsheet/SheetDelegate.kt
@@ -440,7 +440,15 @@
     internal fun createSheetExitAnimator(sheetAnimationContext: SheetAnimationContext): Animator =
         screen.sheetAnimationCoordinator.createSheetExitAnimator(sheetAnimationContext)
 
-    internal fun handleKeyboardInsetsProgress(insets: WindowInsetsCompat) =
-        screen.sheetAnimationCoordinator.handleKeyboardInsetsProgress(insets)
+    internal fun handleKeyboardInsetsProgress(insets: WindowInsetsCompat) {
+        // Patched(CP-14221): do NOT translate the form sheet with the keyboard.
+        // Keyboard avoidance is handled at the content level by
+        // react-native-keyboard-controller (KeyboardStickyView /
+        // KeyboardAwareScrollView). Letting the sheet move too double-handles it
+        // (the sheet AND the sticky footer both shift), which makes the modal
+        // expand to fill the top and misaligns the footer above the keyboard.
+        // This restores the pre-4.25 patched behavior whose original target
+        // functions were removed in the upstream keyboard-handling rewrite.
+    }
 
     internal fun notifyKeyboardAnimationStart() = screen.sheetAnimationCoordinator.notifyKeyboardAnimationStart()
@@ -452,7 +452,8 @@
             bottomSheet: View,
             newState: Int,
         ) {
-            if (newState == BottomSheetBehavior.STATE_COLLAPSED) {
+            if (newState == BottomSheetBehavior.STATE_COLLAPSED ||
+                newState == BottomSheetBehavior.STATE_HIDDEN) {
                 val isImeVisible =
                     WindowInsetsCompat
                         .toWindowInsetsCompat(bottomSheet.rootWindowInsets)
diff --git a/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/ScreenStackFragment.kt b/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/ScreenStackFragment.kt
--- a/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/ScreenStackFragment.kt
+++ b/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/ScreenStackFragment.kt
@@ -388,7 +388,7 @@ class ScreenStackFragment :
         return sheetDelegate.createSheetExitAnimator(sheetAnimationContext)
     }
 
-    private fun createBottomSheetBehaviour(): BottomSheetBehavior<Screen> = BottomSheetBehavior<Screen>()
+    private fun createBottomSheetBehaviour(): BottomSheetBehavior<Screen> = GestureAwareBottomSheetBehavior<Screen>()
 
     private fun resolveBackgroundColor(screen: Screen): Int? {
         val screenColor =
@@ -596,3 +596,27 @@ class ScreenStackFragment :
         return bottomSheetWindowInsetListenerChain!!
     }
 }
+
+// Patched (CP-14976): stock BottomSheetBehavior hands a gesture off to the sheet
+// (onNestedPreScroll moves it, onStopNestedScroll -> shouldHide dismisses it unconditionally
+// when skipCollapsed=true) as soon as the target can't scroll up further *at any point* during
+// the gesture. That lets a drag which starts mid-list reach the top and keep going into a
+// dismiss. Declining nested-scroll participation up front for any gesture (touch or fling) that
+// starts already scrolled means the behavior never receives those callbacks for it, so the
+// ScrollView just scrolls/flings and clamps at top on its own; a gesture that starts at the top
+// is unaffected and still hands off normally.
+private class GestureAwareBottomSheetBehavior<V : View> : BottomSheetBehavior<V>() {
+    override fun onStartNestedScroll(
+        coordinatorLayout: CoordinatorLayout,
+        child: V,
+        directTargetChild: View,
+        target: View,
+        axes: Int,
+        type: Int,
+    ): Boolean {
+        if (target.canScrollVertically(-1)) {
+            return false
+        }
+        return super.onStartNestedScroll(coordinatorLayout, child, directTargetChild, target, axes, type)
+    }
+}
