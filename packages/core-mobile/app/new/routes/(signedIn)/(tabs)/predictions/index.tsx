import React from 'react'
import { View, Text } from 'react-native'

/**
 * Predictions tab root screen — placeholder until CP-13830 (Listing) is built.
 */
export default function PredictionsScreen(): JSX.Element {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Predictions — coming soon</Text>
    </View>
  )
}
