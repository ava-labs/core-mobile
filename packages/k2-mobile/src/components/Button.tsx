import React from 'react'
import { Button as RNButton } from 'react-native'

export const Button = (): JSX.Element => {
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  return <RNButton title="Test button" onPress={() => {}} />
}
